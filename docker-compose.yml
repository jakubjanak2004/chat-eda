services:
  gateway:
    image: nginx:1.27-alpine
    container_name: chat-eda-gateway
    ports:
      - "8080:80"
    volumes:
      - ./gateway/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - user-service
      - search-service
      - ws-service

  user-service:
    build:
      context: .
      dockerfile: user-service/Dockerfile
    container_name: user-service
    env_file:
      - .env
    environment:
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    depends_on:
      postgres:
        condition: service_healthy
      kafka:
        condition: service_started
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9091/actuator/health" ]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 30s
    volumes:
      - user_service_data:/tmp

  search-service:
    build:
      context: .
      dockerfile: search-service/Dockerfile
    container_name: search-service
    env_file:
      - .env
    environment:
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      SPRING_ELASTICSEARCH_URIS: http://elasticsearch:9200
    depends_on:
      elasticsearch:
        condition: service_healthy
      kafka:
        condition: service_started
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9093/actuator/health" ]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 30s
    volumes:
      - search_service_data:/tmp

  ws-service:
    build:
      context: .
      dockerfile: ws-service/Dockerfile
    container_name: ws_service
    env_file:
      - .env
    environment:
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    depends_on:
      kafka:
        condition: service_started
    healthcheck:
      test: [ "CMD", "curl", "-fsS", "http://localhost:9093/actuator/health" ]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 30s
    volumes:
      - ws_service_data:/tmp

  postgres:
    image: postgres:17-alpine
    container_name: chat-eda-postgres
    environment:
      POSTGRES_DB: chat_eda
      POSTGRES_USER: chat
      POSTGRES_PASSWORD: chat
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB" ]
      interval: 5s
      timeout: 3s
      retries: 20
    volumes:
      - postgres_data:/var/lib/postgresql/data

  kafka:
    image: confluentinc/cp-kafka:7.7.1
    container_name: chat-eda-kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: "1"
      KAFKA_PROCESS_ROLES: "broker,controller"
      KAFKA_LISTENERS: "PLAINTEXT://:9092,CONTROLLER://:9093"
      KAFKA_ADVERTISED_LISTENERS: "PLAINTEXT://kafka:9092"
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT"
      KAFKA_CONTROLLER_LISTENER_NAMES: "CONTROLLER"
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@kafka:9093"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: "1"
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: "1"
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: "1"
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: "0"
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      CLUSTER_ID: "abcdefghijklmnopqrstuv"
    volumes:
      - kafka_data:/bitnami/kafka

  elasticsearch:
    # Must match elasticsearch-java on the classpath (Boot 4 → client 9.2.x). ES 8 + client 9 → indices.exists HTTP 400, no body.
    image: docker.elastic.co/elasticsearch/elasticsearch:9.2.6
    container_name: chat-eda-elasticsearch
    environment:
      discovery.type: single-node
      xpack.security.enabled: "false"
      ES_JAVA_OPTS: "-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
      - "9300:9300"
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://localhost:9200/_cluster/health || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 20
    volumes:
      # New name: old volume had ES 8.15 node metadata; ES 9 cannot jump 8.15→9 without 8.19 first (dev: fresh volume is simplest).
      - elasticsearch_data_es9:/usr/share/elasticsearch/data

volumes:
  minio_data:
  postgres_data:
  kafka_data:
  elasticsearch_data_es9:
  user_service_data:
  search_service_data:
  ws_service_data:
